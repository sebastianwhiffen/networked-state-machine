# networked-state-machine

round trip:

client state machine receives input.

that input is executed on the client as well as forwared onto the server.

the server executes that input on the same state machine 
and then sends back an authoratative payload to that reference object







